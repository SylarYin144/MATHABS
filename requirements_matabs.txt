# Python Version Recommendation:
# For the broadest compatibility with all listed dependencies, Python 3.10 or 3.11 is recommended.
# Some older libraries in this list may have more stable pre-compiled versions for these Python releases.
# Note: 'statsmodels' has been updated to a version compatible with Python 3.12.
# If using Python 3.12 and encountering build issues with other packages, consider using Python 3.10/3.11
# or installing C++ Build Tools.
#
bottleneck
factor_analyzer
flask
geopandas
lifelines==0.27.8
matplotlib
numexpr
numpy>=1.21.0,<2.0.0
openpyxl
pandas==2.2.1
pyarrow
scikit-learn==1.3.2
scipy==1.11.4
seaborn
# statsmodels>=0.14.0 is required for Python 3.11+ (including 3.12) due to 'inspect.formatargspec' removal.
# Version 0.14.1 is compatible with numpy<2.0 (requires numpy>=1.21).
statsmodels==0.14.1
plotly
squarify
stemgraphic
matplotlib-venn==1.1.2
# --- Analisis de supervivencia avanzado ---
scikit-survival  # Random Survival Forest (RSF) y metricas de supervivencia
# --- Interpretabilidad de modelos (SHAP) ---
# shap es opcional; si no se instala, la pestaña RSF funciona sin analisis SHAP.
# Para habilitarlo: pip install shap
shap
